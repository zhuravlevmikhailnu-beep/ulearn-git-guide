# A1. —ли€ние

> ƒва состо€ни€ можно объединить через merge, mergetool и commit

> ”частвуют три стороны: current, incoming и base

> »дентичные изменени€ в разных ветках не привод€т к конфликтам

- `git merge <commit>` Ч объединить текущую ветку с другим коммитом
- `git merge --no-ff <commit>` Ч объединить текущую ветку с другим коммитом, причем об€зательно создать новый объедин€ющий коммит
- `git mergetool` Ч разрешить имеющиес€ конфликты
- `git merge --abort` Ч отменить сли€ние

